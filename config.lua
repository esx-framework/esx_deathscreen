Config = {
    zoom = {
	    min = 1, 
	    max = 6, 
	    step = 0.5
    },
	EarlyRespawnTimer = 60000 * 1,  -- time til respawn is available
	BleedoutTimer = 60000 * 10, -- time til the player bleeds out
	Interface = 'custom' -- legacy/custom
}